import { Titulosx20Directive } from './titulosx20.directive';

describe('Titulosx20Directive', () => {
  it('should create an instance', () => {
    const directive = new Titulosx20Directive();
    expect(directive).toBeTruthy();
  });
});
